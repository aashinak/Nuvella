import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com', 'img.freepik.com'], // Add the external domain here

  },
};

export default nextConfig;
